import { HeaderNavigationBar } from "./";
import type { Meta, StoryObj } from "@storybook/react";

const meta: Meta<typeof HeaderNavigationBar> = {
  title: "Components/Header Navigation Bar",
  component: HeaderNavigationBar,
};

type Story = StoryObj<typeof HeaderNavigationBar>;

export const Default: Story = {};

export default meta;
